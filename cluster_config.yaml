# Cluster configuration for SLURM execution
# Usage: snakemake --cluster-config cluster_config.yaml --cluster "sbatch ..."

__default__:
  account: "research-ceg-wm"
  partition: "compute,memory"
  time: "2:00:00"
mem: "8G"
  cpus: 1
  output: "logs/slurm/slurm_%j.out"
  error: "logs/slurm/slurm_%j.err"


sample:
 - test1


